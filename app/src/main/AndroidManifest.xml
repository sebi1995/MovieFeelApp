<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zdroa.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/moviefeel_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.accountmanagement.LoginActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".activities.main.HomeActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".activities.accountmanagement.RegisterActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".activities.personalityquestionnaire.QuestionnaireActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".activities.main.movies.MoviesActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".activities.main.movies.MovieDetailsActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="fullSensor" />
        <activity android:name=".activities.main.movies.WatchedMoviesActivity" />
    </application>

</manifest>